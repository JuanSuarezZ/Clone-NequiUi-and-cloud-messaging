import 'package:flutter/material.dart';

class ArmarioPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Armario page"),
    );
  }
}
